# =============================================================================
# LOCAL DEVELOPMENT - Points to local Supabase (Docker)
# =============================================================================
# Copy this file to .env.local and update values
# Run `supabase start` to get these values

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key

# Site URL (for auth redirects)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# CONTENT EXTRACTION
# =============================================================================
# Parser to use for article content extraction:
# - "firecrawl" (default): Cloud-based, handles JS rendering, anti-bot bypass
# - "readability": Local, free, uses Mozilla's Reader View algorithm
# If FIRECRAWL_API_KEY is not set, falls back to readability automatically
CONTENT_PARSER=firecrawl

# Firecrawl API key (get one at https://firecrawl.dev)
# Required for firecrawl parser, optional if using readability
FIRECRAWL_API_KEY=

# OpenAI API key (for AI summaries and auto-tagging)
OPENAI_API_KEY=

# =============================================================================
# PRODUCTION - Points to cloud Supabase
# =============================================================================
# Set these in Vercel environment variables, NOT in .env.local
# 
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

